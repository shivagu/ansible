---
 - name: install and starting the apache
   hosts: all
   become: true
   task: 
    - name: install http server
      ansible.builtin.package:
          name: httpd
          state: latest
 